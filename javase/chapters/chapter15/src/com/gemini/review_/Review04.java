package com.gemini.review_;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Review04 {
    /*
    题目： 当我们定义 public void print(List<?> list) 时：
我可以从 list 中 get() 元素，取出来的元素类型永远是 Object。

我不可以向 list 中 add() 元素（除了 null），因为编译器无法确定 list 到底装的是什么。
为了保证 类型安全 ，Java 直接禁止了写入操作。
     */
}
