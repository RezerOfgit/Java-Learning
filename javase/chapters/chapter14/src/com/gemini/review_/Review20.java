package com.gemini.review_;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Review20 {
    /*
    考察点：Supplement 10 的结论。
    题目： Collections.copy(dest, src) 执行成功的前提是
    dest.size() >= src.size()。（填 >= 或 == 或 <=）
    注意是 size (元素个数)，不是 capacity (数组容量)。
     */
}
