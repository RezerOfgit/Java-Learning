package com.gemini.review_;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Review28 {
    /*
    Review 28：树化的双重门槛 (HashMap 源码)
考察点：HashMap 底层最容易记混的数字。
题目： HashMap 中，将链表转化为红黑树（Treeify），必须同时满足两个条件：

该链表的长度 >= 8 (默认值)。
数组（Table）的总容量 >= 64 (默认值)。
(如果只满足条件 1 不满足条件 2，系统会选择扩容而不是变树。)
     */
}
