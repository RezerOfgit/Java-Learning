package com.gemini.review_;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Review10 {
    /*
    Review 10：哈希的秘密 (HashSet 去重) 我们知道 HashSet 不允许重复。
    它是怎么判断两个元素是“重复”的？ 它会依次调用哪两个方法？B

A. 只调 equals()
B. 先调 hashCode()，如果哈希值一样，再调 equals()
C. 先调 equals()，如果一样，再调 hashCode()
     */
}
