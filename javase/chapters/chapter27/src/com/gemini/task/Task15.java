package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task15 {
    public static void main(String[] args) {
        /*
        如果我们只想用括号把几个单词括起来进行选择匹配 (Java|C++)，
        但并不想把它们提取出来（不想让它占用内存，提升系统性能），
        我们应该在左括号后面加上什么特殊符号，把它变成非捕获分组？

        // 非捕获分组（不占用内存）
        "(?:Java|C++)"
        //  ↑
        // 加 ?:
         */
    }
}
