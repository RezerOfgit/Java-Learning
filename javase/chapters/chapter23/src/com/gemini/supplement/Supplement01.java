package com.gemini.supplement;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Supplement01 {
    /*
    在刚刚的 Task03 中继续追加代码：

先获取系统当前的类加载器实例（这就是第三个搬运工：Application ClassLoader）：
ClassLoader classLoader = hero.getClass().getClassLoader();

命令这个搬运工，去加载你的目标类，拿到第四面镜子：
Class<?> cls4 = classLoader.loadClass("com.gemini.reflection.Hero");

打印 cls4.hashCode()，你会发现它依然跟前三个一模一样！
     */
}
