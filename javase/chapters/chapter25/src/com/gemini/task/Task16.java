package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task16 {
    /*
    请手写（或默写）一份标准的 druid.properties 配置文件内容，用于连接你本地的 e_commerce 数据库
    （假设账号 root，密码 123456）。
要求至少包含这 4 个最基础的核心参数：驱动类名、URL、用户名、密码。
（注意 key 的名字，Druid 对配置文件的 key 有极其严格的规范，比如不是 user 而是
username）。

driverClassName=com.mysql.jdbc.Driver
url=jdbc:mysql://localhost:3306/gemini_db?rewriteBatchedStatements=true
username=root
password=123456
initialSize=10
minIdle=5
maxActive=50
maxWait=5000
     */
}
