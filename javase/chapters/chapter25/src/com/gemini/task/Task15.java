package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task15 {
    /*

    假设你配置了一个 Druid 连接池，设置了以下参数：initialSize=5 (初始化连接数),
maxActive=20 (最大活动连接数), maxWait=3000 (最大等待时间3000毫秒)。场景提问：
如果此时系统突然迎来了 25 个并发请求（25个线程同时来要连接），请问会发生什么现象？
第 21 到 25 个请求会立刻报错吗？请简要描述 Druid 的处理逻辑。


当 25 个并发请求同时到达时，Druid 连接池的处理逻辑如下：
1.初始阶段：连接池启动时已创建 5 个连接（initialSize=5），这些连接处于空闲状态。
2.前 20 个请求：
前 5 个请求直接获取初始连接。
后续 15 个请求发现空闲连接不足，会触发连接池动态创建新连接，直到达到 maxActive=20 的上限。
因此，前 20 个请求最终都能获取到连接（可能需要短暂等待连接创建）。
3.第 21 至 25 个请求：
此时活动连接数已达到上限（20 个），连接池中没有空闲连接。
这些请求不会立即报错，而是进入等待队列，等待其他连接被归还。
每个请求最多等待 maxWait=3000 毫秒。如果在等待期间有连接释放（例如前 20 个请求中
有操作完成并归还连接），则等待的请求可以获取到连接；如果超过 3000 毫秒仍无可用连接，
则抛出获取连接超时异常。

总结：第 21 到 25 个请求会等待最多 3 秒，而不是立刻报错。
是否最终报错取决于在 3 秒内是否有连接被释放。
     */
}
