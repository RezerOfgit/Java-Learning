package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task18 {
    /*
    在 Apache DBUtils 中，最核心的执行类叫什么？它的底层实际上是对哪个 JDBC 原生
    接口进行了封装？

    在 Apache DBUtils 中，最核心的执行类是QueryRunner。
它的底层主要封装了 JDBC 原生接口 PreparedStatement和 ResultSet，
通过简化参数设置、结果集处理和资源释放等操作，提供更便捷的数据库访问方式。
     */
}
