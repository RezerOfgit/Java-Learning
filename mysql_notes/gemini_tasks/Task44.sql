-- 判断以下关于视图的说法是否正确（直接回复对/错，错的请简单指出原因）：
-- 视图在数据库中占据了大量的物理存储空间，因为它把查询结果存下来了。
-- 错误, 视图只创建了一个映射关系, 实际占用空间非常小.
-- 我们可以基于已经创建好的视图 view_A，再去创建一个新的视图 view_B。
-- 正确
-- 如果我把生成视图的底层基表（Base Table）给 DROP 删除了，这个视图依然可以正常查询出原来的数据。
-- 错误, 视图的数据来自基表的数据, 对基表的操作同样会影响到视图