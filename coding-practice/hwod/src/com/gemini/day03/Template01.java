package com.gemini.day03;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Template01 {
    /**
    1. 当你需要一个“栈”（后进先出，像一摞盘子，或者是浏览器的“后退”键）：

    Java
    // 声明一个字符栈
    Deque<Character> stack = new ArrayDeque<>();
stack.push('a'); // 压入元素
stack.peek();    // 查看栈顶元素（不移除）
stack.pop();     // 弹出并返回栈顶元素
stack.isEmpty(); // 判断是否为空
2. 当你需要一个“队列”（先进先出，像排队买票）：

    Java
    // 声明一个整数队列
    Deque<Integer> queue = new LinkedList<>();
queue.offer(1);  // 排到队尾
queue.peek();    // 查看队头是谁
queue.poll();    // 队头出队
     */
}
