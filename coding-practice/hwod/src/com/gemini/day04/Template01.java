package com.gemini.day04;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Template01 {
    /**
     // 分别代表：上 (-1, 0), 下 (1, 0), 左 (0, -1), 右 (0, 1)
     int[][] dirs = {{-1, 0}, {1, 0}, {0, -1}, {0, 1}};


     // 假设矩阵是 grid，总行数 m，总列数 n。当前走到坐标 (x, y)
     boolean inArea(int x, int y, int m, int n) {
     return x >= 0 && x < m && y >= 0 && y < n;
     }

     boolean[][] visited = new boolean[m][n]; // 默认都是 false


     */
}
