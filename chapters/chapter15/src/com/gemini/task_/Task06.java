package com.gemini.task_;

/**
 * @author Re-zero
 * @version 1.0
 */
/*
背景： 这是一个大坑！请务必区分：
普通方法使用泛型：只是用了类定义的 <T>。
泛型方法：方法自己定义了一个 <E>，跟类的 <T> 半毛钱关系没有，甚至可以“喧宾夺主”。

问题：
代码 car.fly(100) 能通过编译并运行
在 fly 方法里，泛型 E 和类的泛型 T (String) 没有关系
 */
class Car<T> {
    // 🚗 方法 A：普通方法
    // 这里的 T 跟随类的泛型
    public void run(T t) {
        System.out.println("A: " + t);
    }

    // ✈️ 方法 B：泛型方法
    // 注意修饰符 public 后面的 <E>，这是它自己的地盘！
    public <E> void fly(E e) {
        System.out.println("B: " + e);
    }
}

public class Task06 {
    public static void main(String[] args) {
        // 1. 创建对象时，T 被定死为 String
        Car<String> car = new Car<>();

        car.run("宝马"); // A:宝马

        // 2. 调用泛型方法 fly
        // 问：这里我可以传一个 Integer (100) 进去吗？
        car.fly(100);//B:100
    }
}
