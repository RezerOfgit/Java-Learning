package com.gemini.supplement;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Supplement01 {
    public static void main(String[] args) {
        /*
        // 前提：Socket socket 已经有了，代表客户端的连接。
        String destPath = "D:\\cloud\\jay.mp3";

// 1. 请接好 Input 管子 (从哪里抽水？)
        InputStream in = new BufferedInputStream(socket.getInputStream);

// 2. 请接好 Output 管子 (往哪里注水？)
        OutputStream out = new BufferedOutputStream(new FileOutputStream(destPath));

// 3. 请默写核心抽水泵 (字节流搬砖模板)
        byte[] buf = new byte[1024];
        int len;
        while((len = in.read(buf)) != -1) {
            out.write(buf, 0, len);
        }
        */
    }
}
