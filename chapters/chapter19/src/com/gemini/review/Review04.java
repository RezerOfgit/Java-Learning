package com.gemini.review;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Review04 {
    /*
我们今天一直在写 while ((readLen = fileInputStream.read(buf)) != -1) 这行经典代码。
关于这里的 readLen（也就是 read(buf) 方法的返回值），以下说法完全正确的是哪一项？C

A. 它返回的是刚才读上来的那批数据的“字节内容本身”。
B. 它返回的是我们定义的数组长度（比如定义了 new byte[1024]，它就永远返回 1024）。
C. 它返回的是这一次实际读取到的字节数量，如果发现已经没水可抽了，就会返回 -1。
D. 它返回的是这个文件还剩下多少个字节没有读。
     */
}
