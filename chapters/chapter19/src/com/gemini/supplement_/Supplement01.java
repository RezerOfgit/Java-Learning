package com.gemini.supplement_;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Supplement01 {
    /*
    File 类到底是什么？

    File 类的本质：它只是一个**“文件或目录的路径代表”**。
    你可以用它来新建文件、删除文件、看文件有多大（查户口），
    但你绝对不能用它来读写文件里面的内容！

打个比方：File 类就像是“房产证”。你可以看房产证知道房子在哪（路径）、
有多大面积（大小），可以把房子推平（删除）。
但是，你想往房子里搬家具（写数据），房产证办不到，
你得请“搬家公司”（后面要学的 IO 流）来干。
     */
}
