package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task03 {
    /*
    在 Java 的字符串中，你应该怎么写这个正则表达式？为什么在 Java 里不能只写 \. 而是必须写 \\.？
    （请用一两句话简述 Java 编译器转义和正则底层转义的区别）

\\. 经过两关：第一关是 Java 编译器，第二关是正则引擎。Java 编译器先把 \\ 转成 \，
然后正则引擎再把 \. 理解为"匹配实际的点号"。如果只写 \.，Java 编译器会把 \ 当成转义符，
找不到对应的转义规则，直接编译报错。
     */
}
