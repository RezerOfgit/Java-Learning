package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task04 {
    /*
    假设你执行了 SELECT id, info FROM jdbc_test 得到了一个 ResultSet rs。
请默写出遍历这个 rs 结果集并打印每一行 id 和 info 的标准 while 循环代码。
(提示：rs.next() 和 rs.getInt(), rs.getString())


    while (rs.next()) {
        int id = rs.getInt("id");
        String info = rs.getString("info");
        System.out.println("id = " + id + ", info = " + info);
    }
*/
}
