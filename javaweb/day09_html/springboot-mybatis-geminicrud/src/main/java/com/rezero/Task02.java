package com.rezero;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task02 {
    /*
    请用大白话回答：在 MyBatis 中，写 SQL 时用 #{} 和 ${} 有什么本质区别？
    为什么大家都说“非必要绝不使用 ${}”？（提示：结合“预编译”和“SQL 注入”来谈）。

    在 MyBatis 里：
#{}：相当于先给 SQL 打个“占位符” ?，然后用安全的方式把参数值填进去。
就像你用填好金额的支票去付钱，银行（数据库）会严格按照支票上的数字来执行，
不会把数字当成付款指令的一部分。所以它能防止恶意用户通过输入特殊字符
（比如 ' or '1'='1）来改变 SQL 结构，也就是防止 SQL 注入。

${}：相当于直接把参数值拼接到 SQL 字符串里，就像把用户说的内容原样写进合同。
如果用户输入 ' or '1'='1，拼出来的 SQL 就变成了 where name = '' or '1'='1'，
条件永远为真，可能把全表数据都泄露出去。这就是 SQL 注入风险。

为什么“非必要绝不使用 ${}”？
因为一旦用了 ${}，就相当于把数据库的钥匙交到了用户手里。虽然它有时必须用（比如动态传入表名、
列名，#{} 不支持），但只要能用 #{} 的地方绝不用 ${}，这是保护数据安全的基本底线。
     */
}
