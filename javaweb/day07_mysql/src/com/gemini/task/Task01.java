package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task01 {
    /*
    编写 SQL 语句：

1. 查询分类（`category`）为 `'数码'` 且价格（`price`）大于 `100.00` 的所有商品，要求只显示商品名称和价格，并且按照价格**降序 (DESC)** 排列。

2. 查询商品名称（`goods_name`）中包含 `'鼠标'` 两个字的所有商品记录（提示：使用模糊查询 `LIKE`），并按照库存数量升序排列。
     */
}
