package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task04 {
    /*
    编写 SQL 语句：

新建分类表：创建一个 category_info 表，包含 id (主键，自增) 和 category_name (分类名称，VARCHAR)。
重构商品表：给现有的 goods 表增加一个字段 category_id (INT 类型)。
建立纽带：为 goods 表的 category_id 增加一个外键约束 (Foreign Key)，让它关联到 category_info 表的 id 字段上。
     */
}
