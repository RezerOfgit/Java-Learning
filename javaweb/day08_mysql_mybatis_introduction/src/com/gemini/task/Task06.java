package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task06 {
/*    结合视频内容，请用你的话简述以下三个 MyBatis 核心概念的关系：

    实体类 (Entity / POJO)
    Mapper 接口 (DAO)
    SQL 映射 (Annotation / XML)
    大白话提示：你可以把它们比作“货车”、“接货员”和“送货单”。

实体类 (POJO) = 货车：它只规定了“货物”的包装形式（属性），本身不负责运输。比如 User 类有 name、age 字段，就像货车有车厢、轮胎的规格。
Mapper 接口 (DAO) = 接货员：他站在数据库“仓库”门口，发出指令（调用方法），比如“去拿 id 为 1 的用户数据”。但他不知道具体怎么拿，只负责吆喝。
SQL 映射 (XML/注解) = 送货单：单子上写清楚了“去哪张表（仓库位置）”、“拿哪些列（货物清单）”、“怎么装到货车里（结果映射）”。接货员按送货单干活，执行 SQL，并把结果填进货车的对应位置。
关系一句话：
接货员（Mapper）拿着送货单（SQL映射），开着货车（实体类），去数据库仓库装卸货。没有送货单，接货员不知道干什么；没有货车，装回来的数据没处放。


    */
}
