package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task01 {
    /*
编写 SQL 语句：

内连接 (INNER JOIN)：查询所有商品的信息，要求结果中不仅要有商品名 (goods_name) 和价格 (price)，还要显示它对应的分类名称 (category_name)。(提示：利用两张表的 category_id = id 作为连接条件)。
左外连接 (LEFT JOIN)：查询所有的分类名称，以及该分类下的商品名称。要求：即使像“服饰”这种目前没有任何商品的分类，也必须展示在结果里（商品名显示为 NULL）。

     */
}
