package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task05 {
    /*
    在没有 Lombok 之前，我们写一个 User 或 Goods 实体类，要手动写满屏幕的 getter, setter, toString()。有了 Lombok，世界清静了。

请在你的 Spring Boot 项目中：

确保 pom.xml 中引入了 Lombok 依赖。
创建一个名为 Goods 的 Java 实体类（用于对应你数据库里的 goods 表）。
使用 Lombok 的注解（如 @Data, @NoArgsConstructor, @AllArgsConstructor），写出极其简洁的类定义代码（发给我即可，不需要完整的所有字段，写两三个字段示意即可）。
Task03 (MyBatis 核心概念连线 P116-P120)

@Data
@NoArgsConstructor // 无参构造
@AllArgsConstructor // 全参构造
public class User {
        ...
}
     */
}
