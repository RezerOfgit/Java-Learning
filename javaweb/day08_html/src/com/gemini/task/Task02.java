package com.gemini.task;

/**
 * @author Re-zero
 * @version 1.0
 */
public class Task02 {
    /*
    编写 SQL 语句：

标量子查询（单值）：查询出价格高于所有商品“平均价格”的商品名称和价格。(提示：先写一个查平均价格的 SQL，把它用括号括起来，作为主查询的 WHERE 条件)。

列子查询（多值）：查询出分类为 '数码' 或 '家具' 的所有商品信息。

(约束：绝不允许在 goods 表里直接写 WHERE category_id IN (1, 3) 这种硬编码！必须通过子查询去 category_info 表里根据名字查出 ID 集合，再喂给主查询)。
     */
}
